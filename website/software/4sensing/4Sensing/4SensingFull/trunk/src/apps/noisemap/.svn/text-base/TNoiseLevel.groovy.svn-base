package apps.noisemap
import sensing.persistence.core.pipeline.Tuple;

class TNoiseLevel extends Tuple  {
	public enum NoiseClass {LOW, MED, HIGH}
	String cellId;
	double count;
	double sumSpeed;
	double avgSpeed;
	NoiseClass level;
}