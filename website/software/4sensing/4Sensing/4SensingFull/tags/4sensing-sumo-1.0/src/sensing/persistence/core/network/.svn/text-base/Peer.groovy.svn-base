package sensing.persistence.core.network;
import java.awt.geom.*;

public interface Peer {
	public UUID getId();
	public List newPeerLocation(); 	// return [X,Y] Peer coordinates in the space domain - X=Lon, Y=Lat for geographic space
	public boolean accepts(Rectangle2D bounds);
}
